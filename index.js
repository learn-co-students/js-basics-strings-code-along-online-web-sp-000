// Write your code in this file!

// set currentUser to "Chris" that can be changed at a later time.
let currentUser = "Chris"

// set welcomeMessage to include greeting and currentUser
const welcomeMessage = "Welcome to Flatbook, " + currentUser + "!"

// set excitedWelcomeMessage to include greeting and currentUser in all caps.
const excitedWelcomeMessage = "WELCOME TO FLATBOOK, " + currentUser.toUpperCase() + "!"

// set shortGreeting to include greeting and first initial of currentUser.
const shortGreeting = "Welcome, " + currentUser.charAt(0) + "!"
